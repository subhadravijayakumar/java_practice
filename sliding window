sum of the subarray(fixed size)
import java.util.Scanner;
public class Main
{
    public static void main(String args[])
    {
        Scanner sc=new Scanner(System.in);
        int num=sc.nextInt();
        int arr[]=new int[num];
        for(int i=0;i<num;i++)
        {
            arr[i]=sc.nextInt();
        }
        int k=sc.nextInt();
        int result=sum(arr,k);
        System.out.print(result);
        
    }
    public static int sum(int[] arr,int k) 
    {
        int max=0;
        int windowsum=0;
        int windowstart=0;
        for(int i=0;i<arr.length;i++)
        {
            windowsum+=arr[i];
            if(i>=k-1)
            {
                max=Math.max(max,windowsum);
                windowsum-=arr[windowstart];
                windowstart++;
            }
        }
        return max;
        
        
    }
}

Average of the sum 
import java.util.Scanner;
public class Main
{
    public static void main(String args[])
    {
        Scanner sc=new Scanner(System.in);
        int num=sc.nextInt();
        int arr[]=new int [num];
        for(int i=0;i<num;i++)
        {
            arr[i]=sc.nextInt();
        }
        int k=sc.nextInt();
        double average[]=sum(arr,k);
        for(double val:average)
        {
          System.out.print(val+" ");
        }  
        
    }
    public static double [] sum(int arr[],int k)
    {
       double windowsum=0;
       int windowstart=0;
       double result[]=new double[arr.length-k+1];
       for(int i=0;i<arr.length;i++)
       {
           windowsum+=arr[i];
           if(i>=k-1)
           {
               result[windowstart]=windowsum/k;
               windowsum-=arr[windowstart];
               windowstart++;
           }
           
       }
       return result;
    }
}
output
input(format)
9
1 3 2 6 -1 4 1 8 2
5
output
Averages of subarrays of size 5:
2.2 2.8 2.4 3.6 2.8 


import java.util.HashMap;

public class CountAnagrams {

    public static int countAnagrams(String txt, String pat) {
        int count = 0;                     // Step 0: initialize count of anagrams
        int lenTxt = txt.length();         // length of text = 13
        int lenPat = pat.length();         // length of pattern = 3

        // Step 1: Create frequency map of pattern
        HashMap<Character, Integer> patMap = new HashMap<>();
        for (char c : pat.toCharArray()) {
            patMap.put(c, patMap.getOrDefault(c, 0) + 1);
        }
        // patMap = {f=1, o=1, r=1}

        // Step 2: Create frequency map of first window in text
        HashMap<Character, Integer> windowMap = new HashMap<>();
        for (int i = 0; i < lenPat; i++) {
            char c = txt.charAt(i);
            windowMap.put(c, windowMap.getOrDefault(c, 0) + 1);
        }
        // First window "for" → windowMap = {f=1, o=1, r=1}

        // Step 3: Check if first window is an anagram
        if (windowMap.equals(patMap)) {
            count++;                        // window matches → count = 1
        }

        // Step 4: Slide the window through the text
        for (int i = lenPat; i < lenTxt; i++) {
            char newChar = txt.charAt(i);                   // new char enters window
            windowMap.put(newChar, windowMap.getOrDefault(newChar, 0) + 1);

            char oldChar = txt.charAt(i - lenPat);         // old char leaves window
            windowMap.put(oldChar, windowMap.get(oldChar) - 1);
            if (windowMap.get(oldChar) == 0) {
   
             windowMap.remove(oldChar);                // remove char if count 0
            }

            if (windowMap.equals(patMap)) {               // check current window
                count++;                                  // match → increment count
            }
        }

        return count;                                     // return total anagrams
    }

    public static void main(String[] args) {
        String txt = "forxxorfxdofr";
        String pat = "for";
        System.out.println(countAnagrams(txt, pat));     // Output: 3
    }
}


3.	Count of anagrams (or permutations) of a pattern in a string
👉 Find how many substrings of text are anagrams of a given pattern.
Example: txt = "forxxorfxdofr", pat = "for" → Output 3






